# Optional Gotify push notifications.
# Copy to .env.gotify and fill in real values. Do not commit .env.gotify.
GOTIFY_URL=https://gotify.example.com
GOTIFY_TOKEN=your-gotify-app-token
# Set to 1 if reminders should start enabled when notification channels are configured.
NOTIFICATIONS_ENABLED=0
